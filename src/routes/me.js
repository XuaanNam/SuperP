const express = require('express');
const usersController = require('../app/controllers/MeController');
const router = express.Router();

//router.get('/', usersController.index);

module.exports = router;
